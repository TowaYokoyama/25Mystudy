{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // ===== ここから追加 =====
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
    // ===== ここまで追加 =====
  }
}
